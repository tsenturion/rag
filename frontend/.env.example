# Только публичные адреса; ключи LLM и сервисные ключи сюда не помещаются.
VITE_API_BASE_URL=/
# Используется Node-процессом Vite для proxy и не попадает в браузерный bundle.
API_PROXY_TARGET=http://127.0.0.1:8000
